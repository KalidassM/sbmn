<?php
use Illuminate\Database\Migrations\Migration; use Illuminate\Database\Schema\Blueprint; use Illuminate\Support\Facades\Schema; return new class extends Migration { public function up(){Schema::create('expenses',function(Blueprint $t){$t->id();$t->string('voucher_no')->unique();$t->date('expense_date');$t->string('category');$t->text('description');$t->decimal('amount',10,2);$t->string('payment_mode');$t->string('payee')->nullable();$t->string('reference')->nullable();$t->boolean('is_capital')->default(false);$t->timestamps();});} public function down(){Schema::dropIfExists('expenses');} };
